# v0.8.0-beta.1 Release Checklist

## Identity and current state

- Target tag: `v0.8.0-beta.1`; package version: `0.8.0b1`.
- Master: #199; staging and publication: #214.
- Open follow-up: #233 (external Championship response-contract violation,
  accepted as a beta-only limitation).
- Integration PR: #223, `release/214-v0.8.0-beta.1` into `develop`, draft.
- Latest published baseline: `v0.7.0-beta.1`.
- Release type/title: GitHub pre-release,
  `v0.8.0-beta.1 - Outlook Presentation and Reminder Beta`.

This document records preparation, not publication or blanket live acceptance.
Keep #214 and #199 open until the verified signed release actually exists.
Production remains unchanged. Phase 9 #200 remains blocked by Phase 8.

## Scope and documentation

- [x] Functional children #206–#213 are closed on GitHub.
- [x] Media fixes #224, #226, #228, #230, and #234 are integrated into the
      current release branch.
- [x] Package metadata and README identify the Phase 8 candidate.
- [x] Release notes describe presentation, runtime rules, media, migrations,
      source boundaries, known limitations, upgrade, and rollback.
- [x] Confirm the three competition keys in the operator's six-rule profile
      and reconcile all final staging examples against private readback.
- [x] Final documentation accurately records the accepted candidate and
      closes every required staging result without extrapolating samples.

## Local and CI validation

Run against the final prepared tree:

```bash
python -m ruff check .
python -m ruff format --check .
python -m pytest
python -m compileall -q app
python scripts/check_publication_safety.py
docker compose config --quiet
python scripts/validate_multi_instance.py --config-only
docker compose build
python scripts/validate_multi_instance.py
```

Docker validation must use isolated CI projects, placeholder credentials, no
source jobs, disabled providers, and disabled Graph startup validation. Do
not run the CI cleanup procedure against staging or production.

- [x] Ruff lint, format, full pytest, compilation, and publication safety pass locally.
- [x] Compose, image build, and three-instance isolation pass locally.
- [x] #232 demonstrates delayed initialization, a fresh startup after restart,
      and bounded failure with logs; repeated final CI runs pass without retry.
- [x] All five GitHub Actions jobs pass on the actual pushed candidate.
- [x] No unresolved blocking review comments, conflicts, or unrelated diff.

The older release-head CI run 33796358237 passed on `a5496c3`; it does not
validate subsequent preparation changes. The initial #232 failure was
run 33650860739 and passed on controlled rerun; that retry did not fix the race.

## Local preparation evidence — 2026-09-04

Preparation branch: `release/214-beta-release-preparation`, based on release
commit `a5496c36442f829d4129cab2fdd99b27ae914f17`. The operator created
`d65f94f0e6371024d4d52f8ec5510c5d6a780121` for #232; its exact three-file
scope and valid GPG signature were verified locally on 2026-09-04. The
remaining #214 documentation/version changes are uncommitted. The preparation
branch has not yet been published; pushed-candidate CI remains unverified.

- Ruff lint/format, Python compilation, publication safety, local Markdown
  links, and whitespace validation passed.
- Nine startup-readiness regressions passed on native Python 3.13.
- The exact CI Bash loop passed with an intentional 25-second initialization
  delay. Docker reported healthy while the new startup probe remained pending.
- Restarting that same test container again waited for a current startup row;
  the existing row did not satisfy readiness.
- A deliberately failing container exited nonzero and the CI loop returned
  failure with the expected container log, without accepting its health state.
- Compose rendering, image build, and the mandatory three-instance volume,
  network, image, database-probe, and log-isolation assertions passed.
- Docker checks used fresh local CI projects, an empty environment file,
  dummy credentials, no source jobs, and disabled provider/Graph access.

The initial native readiness-test invocation encountered a Windows temporary
folder permission error and passed with a fresh test directory. Full native
and disk-backed container suite attempts were interrupted due to runtime;
these are not passing suite results. The complete final suite passed under
Python 3.13 in the Linux test container: **1,223 passed in 192.96 seconds**.
Only temporary test files used tmpfs; the separate Docker startup/restart checks
used persistent volumes. Source code was copied from tracked working-tree files
and the intended additions without private runtime data. No live acceptance or
GitHub CI pass is inferred from these local checks.

## Live staging acceptance

Use [the Phase 8 runbook](phase-8-staging-validation.md). Existing accepted
samples in #214 need not be repeated unless the corresponding behavior changes.

- [x] Pin the exact candidate, verify staging isolation and a full stopped
      database/media backup, and record upgrade preservation through schema 012.
- [x] Competition, home/away, NFL, and synthetic-final artwork have accepted
      client samples; six remote attachment/CID samples passed.
- [x] Displayed 60-minute, quiet-hour-shifted, suppressed, and competition-only
      reminder samples are recorded in #214.
- [x] Effective previews and runtime update/disable/delete/recreate exercises
      demonstrate the intended policy, including independent participant
      coverage where available; the final six rules survive restart.
- [x] Remove the temporary synthetic final through normal synchronization,
      retire its event rule, preserve audit history, and verify no remote
      orphan event or attachment.
      Retain the completed deletion audit rows; reconcile raw totals against
      `retired_local_audit` using the strict Phase 8 gate (#242), rather than
      deleting history or ignoring unresolved cleanup.
- [x] Media replacement, fallback, and interrupted-operation recovery pass,
      or existing accepted evidence is explicitly referenced.
- [x] Isolated backup restore is byte-identical and SQLite/media-readable
      without Graph or provider access.
- [x] Final provider and canonical isolation evidence is reviewed; #233 remains
      open for a later successful Championship observation under the documented
      beta-only external-provider limitation. Strict validation remains unchanged.
- [x] Full-calendar duplicate/mapping audit passes after cleanup.
- [x] Fresh Phase 8 projection and optional-media evidence passes around
      unchanged cycles:
      zero revision and attachment backlog, stable aggregates, no unnecessary
      Graph writes or attachment uploads.

The final record includes a full 2,088-event remote inventory, restored original
artwork, and exact attachment/media-state stability across calendar runs 8584
and 8585. The strict aggregate command still reports the current Championship
provider failure from #233; that failure is disclosed rather than hidden or
converted into an inferred status.

## Operator-controlled Git and publication

The verified #232 commit `d65f94f` contains exactly:

- `.github/workflows/ci.yml`
- `scripts/check_startup_ready.py`
- `tests/deployment/test_startup_readiness.py`

The earlier #214 preparation commit contained these seven documentation/version files:

- `README.md`
- `pyproject.toml`
- `RELEASE_NOTES_v0.8.0-beta.1.md`
- `docs/v0.8.0-beta.1-release-checklist.md`
- `docs/phase-8-staging-validation.md`
- `docs/runtime-reminder-rules.md`
- `docs/outlook-presentation-reminder-media-contract.md`

The first scope fixes only the Docker startup race owned by #232. The second
scope prepares #214 release identity, documentation, and remaining acceptance;
it does not claim qualification or publication. The branch, staged/unstaged
diffs, and relevant untracked files were inspected before preparing the second
commit; no unrelated changed files were present. English commit messages:

- `ci: wait for current Docker startup record (#232)`
- `docs: prepare v0.8.0-beta.1 release qualification (#214)`

All commits and merges for #214 remain operator-controlled. Before each
commit inspect branch, staged/unstaged diff, and untracked files; include
only the next logical scope and verify its GPG signature afterward.

- [x] The operator committed #232 readiness changes separately; the local
      commit scope and GPG signature are verified.
- [ ] The operator commits the separate #214 release documentation.
- [ ] Verify signed commits, pushed branch, full PR diff, and CI.
- [ ] Maintain PR/issue assignees, labels, milestone, parent/dependencies,
      descriptions, base, and readiness. Use `Refs #214` while publication
      remains incomplete.
- [ ] Integrate preparation into the release branch through a reviewed PR and
      operator merge; recheck #223 metadata and checks afterward.
- [ ] Only after all staging gates pass, mark #223 ready and have the operator
      merge it into `develop`.
- [ ] After staging acceptance, update obsolete pending wording in the final
      release notes before the release PR is approved; preserve actual limitations
      and do not claim unperformed tests.
- [ ] Review the final `develop` → `main` release PR, complete metadata,
      green CI, acceptance criteria, and unresolved reviews.
- [ ] The operator merges the release PR; verify the resulting `main`
      commit, package version, ancestry, and full release diff.
- [ ] Create and verify a signed tag pointing exactly at approved `main`.
- [ ] Publish the GitHub pre-release from that tag and the reviewed notes;
      verify its URL, tag target, title, pre-release flag, and assets.
- [ ] Record final commits, CI, tag verification, and release URL in #214/#199;
      close the issues and milestone only after all criteria are satisfied.
- [ ] Branch cleanup occurs only with explicit authorization.

### Final evidence reconciliation commit

The next logical commit contains exactly these four documentation files:

- `README.md`
- `RELEASE_NOTES_v0.8.0-beta.1.md`
- `docs/phase-8-staging-validation.md`
- `docs/v0.8.0-beta.1-release-checklist.md`

It records completed staging behavior and the explicit beta-only #233 provider
limitation. It changes no application code, schema, configuration, or runtime
data. Use the signed commit message
`docs: record final v0.8.0-beta.1 staging evidence (#214)`.

- [ ] The operator creates and signs the focused final evidence commit.
- [ ] Verify its signature, pushed four-file diff, PR metadata, and CI before
      integrating it into the release branch.

Commands for the operator, only after the verified main gate:

```bash
git tag -s v0.8.0-beta.1 <verified-main-sha> -m "v0.8.0-beta.1 - Outlook Presentation and Reminder Beta"
git verify-tag v0.8.0-beta.1
git push origin v0.8.0-beta.1
gh release create v0.8.0-beta.1 --verify-tag --prerelease --latest=false --title "v0.8.0-beta.1 - Outlook Presentation and Reminder Beta" --notes-file RELEASE_NOTES_v0.8.0-beta.1.md
```

Never publish from an unverified feature branch, bypass signing, downgrade a
live database, or treat the GitHub pre-release as production promotion.
