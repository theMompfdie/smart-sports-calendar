# v0.7.0-beta.1 Release Checklist

## Release identity

- Release: `v0.7.0-beta.1`
- Package version: `0.7.0b1`
- Phase master: #178
- Staging issue: #190
- Documentation preparation: #197
- Publication issue: #198
- Preparation branch: `docs/197-phase-7-release-preparation`
- Release PR: verified `develop` into `main`
- Release type: GitHub pre-release

The release adds exactly one new authority to the published Phase 6 baseline:
the nflverse NFL 2026 regular season. It does not add preseason, postseason,
another provider, another competition, or production promotion.

## Scope and documentation gate

- [x] `pyproject.toml` reports `0.7.0b1`.
- [x] README distinguishes the published `v0.6.0-beta.1` release from the
      `v0.7.0-beta.1` release candidate.
- [x] README reports the bounded NFL scope and 1,048-test candidate baseline.
- [x] Source qualification, ADR 0013, source matrix, and orchestration docs
      describe the final implemented and staged authority.
- [x] nflverse CC BY 4.0 attribution, underlying-rights caveat, private-use
      boundary, and no-redistribution rule are explicit.
- [x] Deployment documents the fixed asset, no-credential setup, six-hour
      minimum poll interval, timeout/retry controls, and one-writer rule.
- [x] NFL three-hour fallback and generic two-hour fallback are documented.
- [x] Upgrade and rollback guidance preserves the project-scoped database and
      never implies an in-place schema downgrade.
- [x] Phase 7 staging evidence records only sanitized aggregate outcomes.
- [x] `RELEASE_NOTES_v0.7.0-beta.1.md` matches the candidate boundary.

## Release boundary

- [x] NFL scope is exactly 272 2026 regular-season games, 32 teams, and weeks
      1–18.
- [x] nflverse `game_id` is primary identity and flex updates preserve event
      correlation.
- [x] Every NFL observation remains partial, incomplete, and removal-disabled.
- [x] Missing or failed observations preserve last-known-good state.
- [x] Preseason, postseason, scores, standings, statistics, betting, rosters,
      injuries, venues, weather, media, and logos remain excluded.
- [x] Raw provider data, private databases, Outlook exports, calendar IDs,
      credentials, private paths, and backup checksums remain unpublished.
- [x] Phase 6 UEFA exclusions and existing authority boundaries remain intact.

## Automated candidate validation

Run from the clean preparation branch:

```bash
python -m ruff check .
python -m ruff format --check .
python -m pytest
python -m compileall -q app
python scripts/check_publication_safety.py
docker compose config --quiet
python scripts/validate_multi_instance.py --config-only
docker compose build
python scripts/validate_multi_instance.py
```

- [x] Ruff lint passes.
- [x] Ruff formatting passes.
- [x] All 1,048 tests pass.
- [x] Python compilation passes.
- [x] Publication-safety validation passes.
- [x] Compose rendering passes without exposing secrets.
- [x] Three isolated Compose configurations pass config-only validation.
- [x] Docker image build and health validation pass locally or in GitHub
      Actions.
- [x] Multi-instance isolation validation passes locally or in GitHub Actions.
- [x] Phase 7 adds no database migration.

## Isolated staging gate

- [x] The reconciled candidate contains exactly nine enabled authorities.
- [x] Strict evidence reports schema 008, 2,088 active events and synchronized
      mappings, and zero pending revisions.
- [x] NFL evidence reports 272 active fixtures, 32 participant mappings, 272
      source fixture mappings, 272 synchronized calendar mappings, and weeks
      1–18.
- [x] Representative private Outlook events show correct Vienna time,
      participants, category, metadata, attribution, flex notice, and
      three-hour duration.
- [x] Unchanged provider and calendar cycles produce no writes or duplicates.
- [x] Controlled provider failure preserves last-known-good state and recovery
      returns to an unchanged successful run.
- [x] Application restart converges without lost or duplicate mappings.
- [x] An isolated restore is byte-identical, passes SQLite quick check, and is
      readable without Graph or provider access.

## Git and GitHub preparation gate

- [x] The final #197 diff contains only reviewed Phase 7 package metadata and
      documentation.
- [ ] The operator creates the required GPG-signed #197 commit.
- [ ] The pushed commit and signature are verified locally and on GitHub.
- [ ] The #197 PR targets `develop`, is ready, has complete description,
      assignee, labels, milestone, issue relationship, and no unrelated diff.
- [ ] Every required GitHub Actions check passes on the #197 PR.
- [ ] The PR has no conflicts or unresolved review comments.
- [ ] The operator merges #197 into `develop`.
- [ ] Issue #197 is closed and #198 is the only remaining release child.

## Final release PR gate

- [ ] `develop` contains the merged #197 result and all required CI checks are
      green.
- [ ] The `develop` to `main` PR references #178 and closes #198 only after the
      release workflow is complete.
- [ ] The release PR has complete metadata and no unrelated commits or files.
- [ ] Ruff, formatting, pytest, compilation, publication safety, Compose,
      Docker, and multi-instance checks pass on the final PR.
- [ ] The operator merges the verified release PR into `main`.
- [ ] The resulting `main` commit and full diff are verified before tagging.

## Signed tag and GitHub pre-release

Create the tag only from the approved `main` commit:

```bash
git tag -s v0.7.0-beta.1 -m "v0.7.0-beta.1 - NFL Regular-Season Beta"
git push origin v0.7.0-beta.1
```

- [ ] The signed tag points exactly to the approved `main` commit.
- [ ] The tag signature and target commit verify locally and on GitHub.
- [ ] GitHub release `v0.7.0-beta.1` uses the signed tag and
      `RELEASE_NOTES_v0.7.0-beta.1.md`.
- [ ] The GitHub release is marked as a pre-release, not latest stable.
- [ ] Release title is `v0.7.0-beta.1 - NFL Regular-Season Beta`.
- [ ] Release metadata and rendered notes are checked after publication.
- [ ] No production stack is changed by publication.

## Administrative completion

- [ ] #197 records its merged PR and final CI result.
- [ ] #198 records the release PR, exact `main` commit, signed tag, and release
      URL.
- [ ] Phase master #178 has no incomplete acceptance criterion after release
      verification.
- [ ] Milestone `v0.7.0-beta.1` has no open issues and is closed.
- [ ] Obsolete Phase 7 branches are removed only with explicit authorization.
- [ ] Production remains unmodified unless a later task explicitly approves
      promotion.

Never delete a persistent volume, overwrite production state, expose private
evidence, weaken the one-writer contract, or create an unsigned release tag.
